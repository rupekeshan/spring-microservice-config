spring:
  data:
    mongodb:
      uri : mongodb+srv://root:lc2wUsSH6jBB2GKm@cluster0.omtoq.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost